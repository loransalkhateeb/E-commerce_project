class CartModel {
  var Id_items;
  var Id;
  var Count;
  var ImageURL;
  var Price;
  var Name;

  CartModel(
      {required this.Id_items,
        required this.Id,
        required this.Count,
        required this.ImageURL,
        required this.Price,
        required this.Name});
}
